const volume = x => x * x * x;
//const area = x => x * x;
// CommonJS 전체 모듈화 moudle.exports = 전체 내용
// => ex 모듈에서는 export default 전체

export default volume;

//export default{
 // volume, //shorthand praperty -key value 값이 똑같으면 한쪽을 생략한다.
//  area
//};